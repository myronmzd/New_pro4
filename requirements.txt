# --- AWS SDK (already in Lambda, but needed for local/devcontainer testing)
boto3>=1.34.0
botocore>=1.34.0

# --- Reddit API
praw>=7.7.1

# --- HTTP requests (fallback / API calls)
requests>=2.31.0

# --- NLP / Sentiment Analysis
vaderSentiment>=3.3.2
textblob>=0.18.0

# --- Data handling
pandas>=2.1.0

# --- Environment variables (local/devcontainers)
python-dotenv>=1.0.1

# --- Logging / utilities
structlog>=24.1.0

# --- Email validation (optional but useful)
# email-validator>=2.1.0
